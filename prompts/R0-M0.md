# R0-M0｜环境、仓库、学习记录基线：学习提示词

把下面这段提示词复制到一个新的 ChatGPT 对话框中使用。

```text
我正在学习 ROS2 + MoveIt2 + ros2_control，当前模块是：

R0-M0｜环境、仓库、学习记录基线

我的身份与目标：
- 我是新手，有浅显 C++ 基础；
- 我的长期目标是应聘机器人机械臂软件相关岗位；
- 请不要百科式堆知识点，而是像工程导师一样带我做；
- 本模块只做环境确认、仓库整理和基线记录，不深入源码、不深入算法。

我的学习仓库：
- GitHub: Mojiu13/ros2_

本模块目标：
1. 确认 ROS2 Jazzy 环境可用；
2. 确认 MoveIt2 可用；
3. 确认 ros2_control 可用；
4. 启动 Panda MoveIt demo；
5. 确认 RViz 能显示机器人；
6. 尝试 Plan / Execute；
7. 用 ros2 命令观察 node / topic / action / controller / parameter；
8. 把结果记录到 README.md、LEARNING_STATUS.md、docs/COMMAND_CHEATSHEET.md、docs/ERROR_LOG.md。

请你按以下方式教我：
1. 先给我本模块的总体目标和边界；
2. 每一步只给我少量命令，让我执行后把输出贴回来；
3. 看到输出后，帮我判断是否正常；
4. 如果报错，先判断属于环境层、launch 层、MoveIt 层、controller 层、RViz 层还是配置层；
5. 每完成一个小阶段，告诉我应该把什么写进 GitHub 文档；
6. 不要过早讲 DDS、OMPL、IK、controller 源码、实时控制细节；
7. 本模块结束时，帮我生成一份可提交到 GitHub 的 R0-M0 总结。

本模块验收命令：

```bash
printenv ROS_DISTRO
ros2 node list
ros2 topic list
ros2 action list
ros2 control list_controllers
ros2 param list /move_group
```

我希望你最终让我能回答：
1. 我的 ROS2 版本是什么？
2. MoveIt2 demo 怎么启动？
3. 启动后有哪些关键节点？
4. 有哪些关键 controller？
5. FollowJointTrajectory action 名称是什么？
6. Plan 和 Execute 是否成功？
7. 如果失败，问题大概发生在哪一层？
8. 我的 GitHub 仓库该如何记录这次学习？

现在请从 R0-M0 第一步开始带我做。
```
