# ENVIRONMENT MANIFEST

状态：Not Verified。ENV-01 中填写真实 Ubuntu/architecture/GPU/Docker/Compose/base image/digest/ROS_DISTRO/RViz/Gazebo/ros_gz/ros2_control/MoveIt/image date/non-root UID-GID。

环境无变化时后续 CORE 不重复更新；真实缺包或版本修复才记录新 image、原因、commit 和验证结果。
